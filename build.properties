# Build properties for Technology Revised
# Edit mod_version (or run ./gradlew incrementVersion) when you add new features.
# The built jar will be named: tech_revised-<mod_version>.jar

mod_version=1.5
